name: CI

on: { push: { branches: '**' } }

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - run: bun i
      - run: bun typecheck
      - run: bun lint
      - run: bun prod