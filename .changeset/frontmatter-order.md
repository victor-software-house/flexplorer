---
"flexplorer": minor
---

Add frontmatter order sort mode — reads `order` field from file frontmatter and sorts the file explorer accordingly. Drag-and-drop writes sequential order values back to frontmatter via processFrontMatter. Auto-initializes missing order fields when switching a folder to this mode.
